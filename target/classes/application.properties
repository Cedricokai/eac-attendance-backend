# Application name
spring.application.name=Eac-Attendance

# Database configuration (using external Railway URL)
spring.datasource.url=jdbc:mysql://trolley.proxy.rlwy.net:39027/railway?useSSL=true&requireSSL=true&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=icfdiVhHPRsKYeIOmOQnzIpOAUPDKeKg

# JPA/Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# CORS configuration
spring.mvc.cors.allowed-methods=GET,POST,PUT,DELETE
spring.mvc.cors.allowed-headers=*
spring.mvc.cors.allowed-origins=http://localhost:5173,https://localhost:5173

# SSL configuration
server.ssl.enabled=false

# WebSocket configuration
websocket.endpoint=/ws
websocket.allowed-origins=*

# STOMP broker configuration (Note: You'll need to set up a proper broker for production)
spring.websocket.stomp.broker.relay.host=localhost
spring.websocket.stomp.broker.relay.port=61613